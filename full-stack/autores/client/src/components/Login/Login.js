import React from 'react';

const Login = () => {
    return (
        <form>
            <input name="email" />
            <input name="password" />
        </form>
    );
};

export default Login;
